# Core runtime dependencies
# Install with: pip install -r requirements.txt

numpy>=1.24.0,<2.0.0
pettingzoo>=1.24.0,<2.0.0
gymnasium>=0.29.0,<1.0.0

# Note: For development dependencies (testing, linting, etc.),
# use: pip install -e ".[dev]"
# See pyproject.toml for full dependency specifications.
